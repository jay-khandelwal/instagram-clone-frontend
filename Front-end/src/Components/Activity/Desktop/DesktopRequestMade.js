import React from "react";
import RequestMade from '../RequestMade';
import BasePopup from '../../Global/BasePopup';

const DesktopRequestMade = (props) => {
    return(
        <BasePopup>
            <RequestMade/>
        </BasePopup>
        );
}

export default DesktopRequestMade;